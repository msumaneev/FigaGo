## 1. База Данных и Привязка Сессий к Профилю

- [x] 1.1 Изменить `DaySessionEntity`: добавить поле `profileId` (важно продумать миграцию Room или `fallbackToDestructiveMigration` если критично, либо добавить nullable / default value).
- [x] 1.2 Изменить `DaySession` доменную модель, добавив `profileId`.
- [x] 1.3 Обновить DAO метод получения списка истории (`observeAllSessions`) так, чтобы он принимал `profileId` в качестве параметра.
- [x] 1.4 Изменить `SessionRepositoryImpl.createSession`: читать идентификатор активного профиля перед созданием `DaySessionEntity` и сохранять его.

## 2. Обновление Истории Треков и UI

- [x] 2.1 Обновить `GetHistoryUseCase` (или `HistoryViewModel`): подписаться на `activeProfile` и запрашивать только сессии текущего профиля. Объединить потоки через `flatMapLatest` или `combine`.
- [x] 2.2 В `HistoryScreen.kt` карточка `SessionCard` должна использовать иконку текущего профиля, а не `ic_notification_preview`.
- [x] 2.3 Переработать `HistoryScreen.kt`: Полностью удалить `TopAppBar` (заголовок "История" и кнопку возврата).
- [x] 2.4 В `HistoryScreen.kt` вынести действие "Очистить всю историю" в виде `item { TextButton(...) }` в самый низ `LazyColumn`.

## 3. Модификация слоя данных (Bulk Save)

- [x] 3.1 Обновить DAO геолокаций: добавить/настроить метод для массовой вставки (Bulk Insert) коллекции точек с использованием аннотации `@Insert`.
- [x] 3.2 Обновить `LocationRepository`: настроить вызов нового bulk-метода из DAO, обернутый в отдельную транзакцию.

## 4. Логика проверок (Domain & Infrastructure)

- [x] 4.1 Создать класс `PreflightChecker` или утилиту `PermissionsHelper` для программной проверки 3 критериев: фоновая геолокация, исключение из `PowerManager`, и статус GPS.
- [x] 4.2 Реализовать формирование Intent для направлений пользователя в системные настройки при обнаружении проблем.

## 5. UI: Экран Диагностики

- [x] 5.1 Разработать `DiagnosticsViewModel`: загрузка статусов проверки при инициализации экрана (`onResume`), хранение состояния списка проблем.
- [x] 5.2 Создать `DiagnosticsScreen` (Compose): отображение индикаторов (✅ / ⚠️) и кнопок "Исправить".
- [x] 5.3 Добавить новый маршрут для `DiagnosticsScreen` в `NavHost`.

## 6. UI: Блокировка старта трекинга

- [x] 6.1 Изменить обработчик кнопки "Старт" на главном экране: перед запуском выполнять вызов проверок из `PreflightChecker`.
- [x] 6.2 Если проверки пройдены — запустить `TrackingService`, если нет — заблокировать старт, показать Toast и перенаправить на `DiagnosticsScreen`.

## 7. UI: Настройка Иконки Профиля (Profile Settings)

- [x] 7.1 Сгенерировать Vector Drawables из предоставленных изображений (`Permobil F`, `Optimus`, и др.) и добавить их в `res/drawable` с именами в стиле `ic_wheelchair_permobil_f.xml`.
- [x] 7.2 Изменить `ProfilePassportViewModel`: добавить State и методы управления выбором `iconId`.
- [x] 7.3 Обновить `ProfilePassportScreen`: добавить выпадающий список (Dropdown) или кастомный пикер рядом с полем ввода имени для выбора иконки из предустановленного набора.

## 8. Сервис: Динамическое пакетирование (Dynamic Batching)

- [x] 8.1 Обновить `LocationRequest` в `TrackingService` (interval=2000, distance=1.5f, Priority=HIGH_ACCURACY).
- [x] 8.2 Добавить метод `updateBatchingMode(isRealtime: Boolean)` в сервис для динамической смены `maxUpdateDelayMillis` (0 или 30000) и перезапуска обновлений.
- [x] 8.3 Обновить `LocationCallback`: передавать весь массив `locationResult.locations` в bulk-save слой.

## 9. Связь Lifecycle UI и Сервиса

- [x] 9.1 Реализовать на экране карты механизм подписки на `LifecycleObserver` (поймать `onResume` и `onPause`).
- [x] 9.2 Интегрировать способ передачи сигнала "Realtime Mode On/Off" в сервис.
## 10. Дашборд: Реорганизация Метрик (Dashboard UI)

- [x] 10.1 В `DashboardScreen` (и `TrackingMetrics`) переместить блок "Дистанция" (Пробег) в центр экрана на место мгновенной скорости.
- [x] 10.2 Заменить показания мгновенной скорости: перенести Время в пути и Среднюю скорость наверх экрана (по бокам). Мгновенную скорость можно удалить.

## 11. Точное форматирование дистанции

- [x] 11.1 Изменить доменную бизнес-логику форматировщика `FormatMetricsUseCase` (или добавить отдельную функцию): при расстоянии < 1 км возвращать строку типа "250 м" (или "820 футов" для миль). При расстоянии > 1 км возвращать строку типа "1 км 200 м".
- [x] 11.2 Обновить `TtsAnnouncerService.buildAnnouncementText()` для точной генерации фразы: если `< 1000м` -> "пробег X метров", если `>= 1000м` -> "пробег Y километров Z метров" с правильным склонением числительных на русском языке (для миль - мили/футы). Округление до целых километров отменить.

## 12. Исправление UI и Релиз
- [x] 12.1 Обновить `versionCode` до 6 и `versionName` до "1.3.1" в `build.gradle.kts` для успешного обновления APK на устройстве.
- [x] 12.2 Убедиться, что `strings.xml` и `ProfilePassportScreen.kt` содержат корректные исправления верстки и лимитов строк (Компактный выпадающий список для иконки с `width(80.dp)` и уменьшенный текст `Макс. скорость` с описанием `Порог для переключения...`).
- [x] 12.3 Пересобрать приложение (assembleDebug).
- [x] 12.4 Выпустить v1.3.2: Перенести выпадающий список выбора иконки также и на главный экран настроек (`SettingsScreen.kt`), так как часть карточки дублируется, и пользователь смотрел именно туда.
