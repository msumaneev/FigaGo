## 1. Рефакторинг управления состоянием (SettingsViewModel)

- [x] 1.1 Добавить свойство `originalState` в `SettingsViewModel`. При `loadSettings()` сохранять значения и в `_uiState`, и в `originalState`.
- [x] 1.2 Добавить `StateFlow` под названием `isDirty` (или вычисляемое свойство), которое сравнивает текущий `_uiState.value` и `originalState`.
- [x] 1.3 Удалить методы немедленного сохранения (вызовы `settingsRepository.set...`) из всех функций-сеттеров (напр. `setTtsTimeInterval`), оставив только обновление локального `_uiState`.
- [x] 1.4 Написать функцию `saveChanges()`, которая запускает `viewModelScope.launch`, последовательно сохраняет все поля из `uiState` в `SettingsRepository`, и затем обновляет `originalState`.

## 2. Разработка барабана выбора чисел (Wheel Picker)

- [x] 2.1 Разработать кастомный `WheelNumberPicker` (барабан) на Compose (на базе `LazyColumn` и `rememberSnapFlingBehavior` из Android Foundation), позволяющий скроллить числа (например от 1 до 99).
- [x] 2.2 Убрать текстовое поле `OutlinedTextField` из всех настроек целых чисел (Интервал мин, GPS сек, Авто-пауза).
- [x] 2.3 Внедрить `WheelNumberPicker` (или диалог с ним) в интерфейс настроек для визуального выбора значений без использования клавиатуры.

## 3. Доработка UI Настроек (SettingsScreen)

- [x] 3.1 Реализовать закрепленный `BottomAppBar` или плавающую кнопку (FAB) "Сохранить". Сделать её кликабельной только когда `isDirty == true`. При успешном `saveChanges()` показывать Snack/Toast или скрывать кнопку.
- [x] 3.2 Внедрить `BackHandler(enabled = isDirty)` для перехвата системной кнопки Назад. Вызывать показ диалога "Несохраненные изменения".
- [x] 3.3 Изменить поведение кнопки "Назад" в верхней панели (`TopAppBar`), чтобы она триггерила тот же флоу проверки, что и `BackHandler`.
- [x] 3.4 Сверстать `AlertDialog` с текстом "Сохранить изменения перед выходом?" и кнопками: "Сохранить", "Не сохранять" и "Отмена". Обеспечить корректную навигацию `onNavigateBack()` при выборе соответствующих действий.
