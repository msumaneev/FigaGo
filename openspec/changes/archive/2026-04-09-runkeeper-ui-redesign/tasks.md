## 1. Рефакторинг навигации (Bottom Navigation)

- [x] 1.1 Переработка `MainActivity` / `NavHost` для использования `Scaffold` с `BottomNavigationBar`. Создание 4-х вкладок: Я (карта), История, Старт (запись), Настройки.
- [x] 1.2 Извлечение логики старого `MainScreen` в разбитые компоненты (`MapScreen`, `DashboardScreen`), чтобы разделить карту и панель управления.

## 2. Разработка Дашборда записи трека (Dashboard Screen)

- [x] 2.1 Верстка центральной информационной панели: огромный таймер (`displayLarge`), пробег, перенос `InteractiveLedIndicators` и блока `ForecastDisplay` (прогноз остатка) в новый `Column`.
- [x] 2.2 Разработка панели управления (Action Buttons): Круглая кнопка "Пауза" (когда трек пишется) и две кнопки "Старт/Возобновить" и "Стоп/Финиш" (когда трек на паузе) вместо текстов.
- [x] 2.3 Связывание `DashboardScreen` с `MainViewModel`: корректное отображение и переключение стейтов (Idle -> Recording -> Paused) по кнопкам.

## 3. Авто-пауза при превышении скорости

- [x] 3.1 Добавление настройки в `SettingsRepository` и `SettingsScreen`: "Авто-пауза при скорости > X км/ч" (с гибким выбором от 5 до 30 км/ч для учета колясок с лимитом 6-7 км/ч).
- [x] 3.2 Модификация `TrackingService.kt`: получение значения `location.speed` (в м/с), перевод в км/ч и добавление внутреннего счетчика последовательных превышений.
- [x] 3.3 Интеграция авто-паузы: если скорость превышена в 3-х последовательных координатах, вызываем функцию паузы в сервисе без завершения сессии (обновление `DayState.PAUSED`).

## 4. Интеграция со шторкой (Уведомления и TileService)

- [x] 4.1 Подготовка референсов-иконок к Android-формату: нарезка, подгонка размеров и добавление в `res/drawable` (для кнопок) и `mipmap` (для икоки приложения).
- [x] 4.2 Модификация `TrackingService.kt`: добавление `NotificationCompat.Action` в `buildNotification` (Пауза, Стоп, Лампочка), обновление UI уведомления при смене стейта в активной сессии.
- [x] 4.3 Создание `FigaGoTileService` (наследник `TileService` API 24+): реализация переключения состояния записи из шторки (Pause / Resume) и регистрация в `AndroidManifest.xml`.

## 5. Завершение и тестирование

- [x] 5.1 Проверка сохранения состояния между вкладками: переключение на "Историю" во время записи не должно убивать трек на Дашборде.
- [x] 5.2 Тестирование графического интерфейса: убедиться, что все цифры и индикаторы (LED) крупно и контрастно читаются.
- [x] 5.3 Интеграционное тестирование действий в шторке: взаимодействие с "быстрыми" кнопками во время трекинга.
