## 1. Data Layer (Room & DataStore)

- [x] 1.1 Создать сущность `Profile` (type, name, iconId, maxSpeed, maxMileage, ledCount, ledDistances) и `ProfileDao`.
- [x] 1.2 Добавить `TypeConverter` для сохранения списка дистанций лампочек (`List<Float> <-> JSON`).
- [x] 1.3 Добавить поле `isTransport` в сущность `TrackPoint`.
- [x] 1.4 Добавить `profileId` в сущность `Track`.
- [x] 1.5 Написать миграцию БД: создать дефолтный профиль "Моя коляска" из старых настроек, проставить `profileId` старым трекам, заполнить `isTransport = false` для всех старых точек.
- [x] 1.6 Добавить хранение `activeProfileId` и `unitSystem` (Km/Miles) в настройки `DataStore`.
- [x] 1.7 **Per-profile DataStore**: все настройки хранятся с префиксом `p{profileId}_` для изоляции профилей.

## 2. Domain Layer & Utilities

- [x] 2.1 Создать `ProfileRepository` для управления и переключения активного профиля.
- [x] 2.2 Рефакторинг конвертеров дистанции/скорости: все математические функции должны возвращать значения с учетом `unitSystem` (отображение в милях, расчеты в км/м).
- [x] 2.3 Адаптировать калькуляторы предсказания батареи на использование профильных (Passport) метрик вместо глобальных.

## 3. Background Service & Location (Day Logging)

- [x] 3.1 Полностью переписать `LocationService`: использовать `FusedLocationProviderClient` с интервалами `5000ms`, `setMinUpdateDistanceMeters(1.5f)`, `setMaxUpdateDelayMillis(15000ms)`. Выпилить логику ручных `WakeLock`.
- [x] 3.2 Реализовать конечный автомат (State Machine) Транспорта: анализ `location.speed()`, агрегация (5 измерений подряд > Profile.maxSpeed + 20%), динамическое переключение статуса `WHEELCHAIR` <-> `TRANSPORT` и простановка флага новым точкам.
- [x] 3.3 Обновить StateFlow метрики: расчет "Общей пройденной на коляске дистанции" (без зеленого сегмента), "Мгновенной скорости" и "Средней скорости текущего сегмента".
- [x] 3.4 Выпилить поддержку ручной/авто паузы геолокации и ручного закрытия "дня" по таймауту.

## 4. UI Layer (Compose)

- [x] 4.1 Создать `ProfilePassportScreen`: форма создания/редактирования с выбором Электрическая/Ручная коляска (`AnimatedVisibility` блоков батареи) и колесами-барабанами для настройки уровней (LED levels).
- [x] 4.2 Модернизировать экран записи (Старт): добавить горизонтальную карусель навигации профилей.
- [x] 4.3 Обновить дашборд трекинга: добавить крупные виджеты "Моментальная скорость", "Средняя скорость" и "Пробег профиля за день". Внедрить поддержку Миль/Км на уровне ViewModel.
- [x] 4.4 Обновить компонент Карты (History & Dashboard): разбивать `TrackPoint` на отельные `Polyline` при разрывах во времени (прерывание трека) или смене флага `isTransport` (кастомная зеленая/оранжевая отрисовка).
- [x] 4.5 Навести порядок в Настройках приложения: удалить меню интервалов GPS и авто-паузы, перенести настройки батареи в Паспорта профилей, добавить переключатель Единиц (Km/Mi).

## 5. Profile Architecture Refactoring (v2)

- [x] 5.1 **Глобальная карусель профилей**: вынести из DashboardScreen в NavHost/Scaffold. Карусель видна на всех основных вкладках (Старт, История, Карта, Настройки).
- [x] 5.2 **Диалог смены профиля**: при переключении профиля во время активного трека — AlertDialog с предложением завершить трек и стартовать новый.
- [x] 5.3 **Объединённый экран настроек**: паспорт коляски (название, тип, скорость, батарея) интегрирован в SettingsScreen. Порядок: Паспорт → Батарея → Метрики → Голос → TTS → Язык.
- [x] 5.4 **Per-profile DataStore**: все ключи настроек с префиксом `p{id}_`. SettingsViewModel автоматически перезагружается при смене профиля.
- [x] 5.5 **Иконка электро-коляски**: кастомная иконка (коляска с пламенем) для ELECTRIC профилей в карусели, навбаре и шторке уведомлений.
- [x] 5.6 **Динамический навбар**: закладка «Я» показывает имя и иконку активного профиля.
- [x] 5.7 **Seed-данные**: дефолтный профиль «Моя коляска» при первой установке (RoomDatabase.Callback.onCreate).
- [x] 5.8 **Клик на активную плитку** → навигация в Настройки (вместо открытия ProfilePassportScreen).
- [x] 5.9 **Прогноз пробега**: системный цвет темы для текста (убраны зелёный/оранжевый/красный + пульсация).
- [x] 5.10 **Стартовый экран**: DASHBOARD вместо MAP.

