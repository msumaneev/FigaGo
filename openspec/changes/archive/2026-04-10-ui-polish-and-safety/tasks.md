## 1. Ресурсы и Иконки

- [x] 1.1 Пересобрать иконку `ic_notification_preview.png` для системной шторки (обрезать края `bounding box` и увеличить толщину линий).
- [x] 1.2 Заменить дефолтную иконку на экране "История" (список треков) на готовую иконку коляски.

## 2. Экран Настроек (Settings UI)

- [x] 2.1 Изменить логику "Сохранить": убрать `popBackStack()`, реализовать мгновенный сброс буфера `isDirty` и перерисовку барабана.
- [x] 2.2 Переработать элемент "Автозакрытие дня" на два независимых компонента `WheelNumberPicker` (Часы и Минуты).
- [x] 2.3 Переверстать элемент авто-паузы, подняв текст "0=выключено" под основной заголовок настройки.
- [x] 2.4 Добавить новый барабан для настройки "Предупреждать о точке невозврата за X км" и привязать к DataStore.

## 3. Фоновый сервис и Уведомления

- [x] 3.1 Реализовать принудительное обновление уведомления (Notification) при постановке/снятии с паузы в `TrackingService.kt` (вызов `updateNotification()`).
- [x] 3.2 Добавить логику перехвата "Точки невозврата": вычисление `calculatedMaxDistance / 2`, вычитание зазора предупреждения и триггер TTS. Внедрить блокирующий флаг `isPoNRWarningPlayed`.
- [x] 3.3 Применить функции округления в генераторе фраз TTS: математическое округление `dist.roundToInt()` для пройденного и `remaining.toInt()` для остатка.

## 4. Дата-слой Истории (Room & Domain)

- [x] 4.1 Изменить UseCase финализации трека: если итоговая дистанция меньше 500 метров, прерывать сохранение или удалять объекты `LocationEntity` и `TrackEntity`.
- [x] 4.2 Добавить функции удаления конкретного трека `deleteTrack(id)` и полного удаления `clearAllTracks()` в Дао и Репозиторий.
- [x] 4.3 Прокинуть эти методы в `HistoryViewModel`.

## 5. UI Отрисовки карт и Истории

- [x] 5.1 Добавить кнопочки корзины к элементам треков и кнопку "Очистить историю" в функционал `HistoryScreen` (с отображением AlertDialog перед удалением).
- [x] 5.2 Починить биндинг координат локации на карту в режиме "Я" (Live): подключиться к Flow и передавать `LatLng` списком в компонент `Polyline` Google-карт.
- [x] 5.3 Реализовать корректный парсинг или выборку из Room точек для просмотра старого трека и отрисовку их как `Polyline` в History-экране.
